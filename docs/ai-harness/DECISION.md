# 공통 하네스 도입 결정

- 요청: Claude Code와 Codex를 번갈아 사용하며 모든 연결 저장소를 관리한다.
- 결정: AGENTS.md를 공통 진입점으로 사용하고 CLAUDE.md에서 가져온다.
- 기존 Claude 프로젝트 지식은 같은 디렉터리의 PROJECT_CONTEXT.md로 이동해 상대 경로를 유지한다.
- 기존 도메인 제약·운영 설정·애플리케이션 모델·MCP 인증은 유지한다.
- GSD 전용 진입 요구는 동일한 Markdown 계획·검증 기록을 유지하는 도구 중립 절차로 바꾼다.
- 하네스 자체 검증과 애플리케이션 검증은 별도로 보고한다.
- 기존 작업 상태·로드맵의 사본을 생성하지 않고 작업 인계 기록에서 연결한다.
- 공유 실행 파일은 .harness/에 두어 애플리케이션 패키지 검색과 분리한다.
- 하네스 버전: 1.0.0. 파일 변경은 Git diff로 검토하고 자동 덮어쓰기 동기화는 사용하지 않는다.
